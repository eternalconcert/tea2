// Functions in this file are called by the init code in tea binary.
// This file is compiled into a shared library and loaded at runtime, so it can be edited without recompiling tea itself.

print("Testing init code");
